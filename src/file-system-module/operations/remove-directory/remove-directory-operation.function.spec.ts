import { removeDirectoryOperation } from './remove-directory-operation.function';

describe('removeDirectoryOperation', () => {
  it('is a function', () => expect(typeof removeDirectoryOperation).toBe('function'));
});
