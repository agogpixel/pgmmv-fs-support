import { createFileSystemModule } from '@agogpixel/pgmmv-fs-support/src/file-system-module';

describe('createFileSystemModule', () => {
  it('is a function', () => expect(typeof createFileSystemModule).toBe('function'));
});
